#!/bin/zsh
# acuopen: AtCoder URL Open
USAGE="acuopen <contest_ids> <task_ids>"

if [ $# -lt 2 ]; then
    echo $USAGE
    exit 1
fi

for u in $(acugen "$1" "$2"); do
    open -a "Google Chrome" $u
done
